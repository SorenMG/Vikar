import initialState from '../store/jobs';

export default {
  namespace: 'jobs',

  /**
   *  Initial state
   */
  state: initialState,

  /**
   * Effects/Actions
   */
  effects: (dispatch) => ({}),

  /**
   * Reducers
   */
  reducers: {},
};
